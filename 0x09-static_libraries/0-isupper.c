#include "main.h"

/**
 * _isupper - print all in uppercase letters
 * @c: char to check
 *
 * Return: from 0 else from 1
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	return (1);
	else
	return (0);
}
